# Grid Move Selector

Chooses safe moves on a grid and selects the move or moves closest to the apple position.

## Concepts used

- 2D vectors
- structs
- bounds checking
- Euclidean distance
- sorting tie results

## Build and run

```bash
g++ -std=c++17 main.cpp -o grid-move-selector
./grid-move-selector
```
