# Sum N Numbers in Pep Assembly

Reads `N`, then reads `N` numbers, adds them together, and prints the sum.

## Concepts used

- labels
- memory variables
- loop control
- accumulator-based arithmetic
- console input/output

## Run

Open `main.pep` in a compatible Pep simulator and run it there.
