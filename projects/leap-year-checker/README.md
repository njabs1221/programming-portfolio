# Leap Year Checker

Checks whether a year is a leap year.

## Rule

A year is a leap year if it is divisible by 400, or if it is divisible by 4 but not divisible by 100.

## Build and run

```bash
g++ -std=c++17 main.cpp -o leap-year-checker
./leap-year-checker
```
